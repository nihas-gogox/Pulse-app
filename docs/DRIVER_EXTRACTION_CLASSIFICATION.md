# Driver Extraction — Classification (Phase 1, step 1)

Generated by `node scripts/driver-extraction-classify.mjs`. **Do not edit it by hand.** Manual decisions go in `docs/DRIVER_EXTRACTION_OVERRIDES.json` as `{ "<file>": { "cls": "...", "why": "..." } }`; then re-run the script.

**Gate:** no code moves until the REVIEW count is 0 and Nihas approves this file.

## Gate

| Check | Result |
|---|---|
| REVIEW = 0 | ✅ |
| Package-rule violations = 0 | ✅ |
| Approved decisions applied | D1, D11, D12, D13, D14, D15, D2, D3, D4, D5, D8, D1, D9, D10 |
| Files no longer needed at runtime once D9/D2 rewrites are done | 125 |

## Summary

| Class | Files |
|---|---:|
| DRIVER_ONLY | 249 |
| SHARED_CORE | 86 |
| SHARED_DOMAIN | 275 |
| SHARED_UI | 45 |
| SHARED_FEATURES | 64 |
| MAIN_ONLY | 481 |
| REVIEW | 0 |
| **Total (driver closure + seeds, non-test)** | **1200** |

The contract check took 3 passes. `MAIN_ONLY` listed here = files the driver imports types from only (rule Y1). Files outside the driver closure are also MAIN_ONLY and aren't listed. `MAIN_ADAPTER`, `DRIVER_ADAPTER` and `DELETE` only appear through manual overrides.

## Decisions needed (REVIEW grouped)

| Bucket | Question | Files | Biggest areas |
|---|---|---:|---|

### What unblocks D6 (root blockers)

D6 files are blocked only by another REVIEW file. Resolving the root decision unblocks them.

| Root bucket | D6 files unblocked | Top root files |
|---|---:|---|

## Rules used

| Rule | Meaning | Files |
|---|---|---:|
| `C2` | session/network/language infrastructure (auth split per plan Phase 2) | 2 |
| `F1` | shared business UI (D1 | 44 |
| `F2` | feature service/util/hook/domain code | 189 |
| `F2←pulled` | feature service/util/hook/domain code | 2 |
| `K1` | design/presentation constants | 8 |
| `L1` | asset/lottie module | 2 |
| `L2` | renders UI | 2 |
| `L3` | business-named shared module | 26 |
| `L3←pulled` | business-named shared module | 2 |
| `L4` | generic infrastructure/helper | 60 |
| `L4←pulled` | generic infrastructure/helper | 2 |
| `L4→V1` | generic infrastructure/helper | 4 |
| `MANUAL:D1` | signup shell/keypad UI (rendering files only | 2 |
| `MANUAL:D11` | shared business code for now | 1 |
| `MANUAL:D12` | client-side perf counters, infrastructure | 1 |
| `MANUAL:D13` | renders party/business avatars (needs avatar data from domain) | 2 |
| `MANUAL:D14` | rendered by the shared LeafletMap family | 1 |
| `MANUAL:D15` | one-line re-export | 1 |
| `MANUAL:D1→fallback` | signup shell/keypad UI (rendering files only | 13 |
| `MANUAL:D2` | platform identity/workspace | 41 |
| `MANUAL:D3` | shared context (D3 amended by D11 | 2 |
| `MANUAL:D4` | next to lib/avatarUpload | 6 |
| `MANUAL:D5` | pure viewport math, maps infrastructure | 1 |
| `MANUAL:D8` | used by shared LeafletMap family | 2 |
| `N1` | only reachable from driver code | 80 |
| `Q1` | query hooks / domain query keys | 14 |
| `S1` | driver seed, no live main-app importer | 169 |
| `S2` | LeafletMap family is used by the dispatcher map (plan §1) | 7 |
| `T1` | shared domain types | 2 |
| `U1` | shared component | 28 |
| `U1→D13` | shared component | 3 |
| `Y1` | driver imports only its types (extract types in Phase 2 | 481 |

## DRIVER_ONLY (249)

| File | Rule | Reason |
|---|---|---|
| `app/(driver)/_layout.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/available-loads/[indentId].tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/available-loads/_layout.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/available-loads/index.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/become-fleet-owner.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/capacity-story.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/chat.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/commerce-mission/[tripId].tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/commerce-mission/_layout.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/control.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/dco-status.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/documents.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/expense-capture.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/general-expense.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/index.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/level-progression.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/loading.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/market-awards.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/my-bids.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/my-fleet/[vehicleId].tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/my-fleet/_layout.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/my-fleet/add.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/my-fleet/index.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/notifications.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/passbook/[orgId].tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/passbook/_layout.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/passbook/history.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/passbook/loading.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/pending-earnings.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/profile.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/requests.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/salary-request/[id].tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/salary-request/_layout.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/salary-request/index.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/settings.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/stories.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/trip-history/_layout.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/trip-history/index.tsx` | S1 | driver seed, no live main-app importer |
| `app/(driver)/wallet.tsx` | S1 | driver seed, no live main-app importer |
| `app/driver-sign-in.tsx` | S1 | driver seed, no live main-app importer |
| `app/driver-signup.tsx` | S1 | driver seed, no live main-app importer |
| `app/driver-trip/[tripId].tsx` | S1 | driver seed, no live main-app importer |
| `app/driver-trip/_layout.tsx` | S1 | driver seed, no live main-app importer |
| `app/onboarding/driver.tsx` | S1 | driver seed, no live main-app importer |
| `components/JobRequestCard.tsx` | N1 | only reachable from driver code |
| `components/driver/DriverBrandMark.tsx` | S1 | driver seed, no live main-app importer |
| `components/driver/DriverHeader.tsx` | S1 | driver seed, no live main-app importer |
| `components/driver/DriverHeaderTripOpsButtons.tsx` | S1 | driver seed, no live main-app importer |
| `components/driver/DriverInviteCard.tsx` | S1 | driver seed, no live main-app importer |
| `components/driver/DriverInviteModal.tsx` | S1 | driver seed, no live main-app importer |
| `components/driver/DriverInviteModalGate.tsx` | S1 | driver seed, no live main-app importer |
| `components/driver/DriverSelfAvatar.tsx` | S1 | driver seed, no live main-app importer |
| `components/driver/DriverSubScreenHeader.tsx` | S1 | driver seed, no live main-app importer |
| `components/driver/DriverTabBar.tsx` | S1 | driver seed, no live main-app importer |
| `components/driver/DriverTripSheetLayout.tsx` | S1 | driver seed, no live main-app importer |
| `components/driver/LiveRouteInfoCard.tsx` | S1 | driver seed, no live main-app importer |
| `components/driver/NeededByCalendar.tsx` | S1 | driver seed, no live main-app importer |
| `components/driver/TripListAssignerRow.tsx` | S1 | driver seed, no live main-app importer |
| `constants/DriverTypography.ts` | N1 | only reachable from driver code |
| `contexts/DriverInviteModalContext.tsx` | S1 | driver seed, no live main-app importer |
| `contexts/DriverTripOpsContext.tsx` | S1 | driver seed, no live main-app importer |
| `features/auth/components/EditProfileModal.tsx` | N1 | only reachable from driver code |
| `features/auth/signup/signUpDriverLottieAssets.ts` | N1 | only reachable from driver code |
| `features/chat/components/ChatFeedbackCard.tsx` | N1 | only reachable from driver code |
| `features/chat/components/driver/DriverChatSlackInbox.tsx` | N1 | only reachable from driver code |
| `features/chat/components/driver/DriverChatSlackThread.tsx` | N1 | only reachable from driver code |
| `features/chat/components/driver/DriverShipperFeedbackModal.tsx` | N1 | only reachable from driver code |
| `features/chat/contexts/DriverChatContext.tsx` | N1 | only reachable from driver code |
| `features/chat/hooks/useDriverChatConversationsQuery.ts` | N1 | only reachable from driver code |
| `features/chat/hooks/useDriverChatMessagesQuery.ts` | N1 | only reachable from driver code |
| `features/chat/hooks/useDriverChatSubscription.ts` | N1 | only reachable from driver code |
| `features/chat/utils/chatImageUpload.util.ts` | N1 | only reachable from driver code |
| `features/chat/utils/driverChatInboxPreview.util.ts` | N1 | only reachable from driver code |
| `features/chat/utils/driverChatMessageCache.util.ts` | N1 | only reachable from driver code |
| `features/driver/commerce-mission/DriverCommerceMissionEntry.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/commerce-mission/DriverCommerceMissionScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/commerce-mission/DriverCommerceMissionView.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/commerce-mission/driverCommerceMissionLabels.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/commerce-mission/driverTripStopOrders.types.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/commerce-mission/fetchDriverTripStopOrders.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/commerce-mission/index.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/commerce-mission/normalizeDriverTripStopOrders.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/commerce-mission/useDriverCommerceMission.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/communication/DriverCommunicationProvider.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/communication/constants.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/communication/hooks/useDriverChatSystem.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/communication/hooks/useDriverLocationStream.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/communication/hooks/useDriverPaymentListener.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/communication/index.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/communication/publishDriverPaymentBroadcast.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/components/AddOwnerVehicleScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/AvailableLoadDetailScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/AvailableLoadsScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/BecomeFleetOwnerScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/CapacityStoryComposerScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DcoStatusScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DriverDailySummaryCard.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DriverDashboard.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DriverDashboardMapPreview.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DriverDocumentGalleryPreview.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DriverExpenseCaptureFab.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DriverExpenseCaptureScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DriverGeneralExpenseScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DriverMissionStopsList.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DriverPodCompletionPage.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DriverTripExpenseLogSection.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DriverTripFlowCard.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DriverTripHistoryDetailScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DriverTripOperationsTab.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/DriverWorkOpportunityCard.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/MarketAwardsScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/MarketLoadBidSheet.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/MissionCardLayout.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/MyBidsScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/MyFleetScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/OwnerVehicleDetailScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/OwnerVehicleDocumentsSection.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/PendingEarningsTripCard.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/PendingSalaryRequestCard.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/PilotRelationshipSummary.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/TripDetailSettlementPanel.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/components/TripPaymentAmountGrid.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/execution/driverStopExecution.types.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/execution/fetchDriverStopExecution.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/execution/normalizeDriverStopExecution.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/execution/resolveDriverStopTransition.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/execution/transitionDriverStopExecution.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/hooks/useAdaptiveTripLocationPingLoop.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/hooks/useDriverHomeInvites.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/hooks/useDriverHomePerfMarks.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/hooks/useDriverMapLivePositionWatch.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/hooks/useDriverPendingEarnings.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/hooks/useDriverStopExecution.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/hooks/useDriverTripOpsActions.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/hooks/useDriverTripSettlement.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/DriverJobCard.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/DriverJobCardPending.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/DriverMultiOrderJobCard.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/DriverStopVerificationScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/DriverTripCompletionScreen.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/attachOrdersToStop.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/commerceProductImageUrl.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/driverRoutePlanMap.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/multiOrderStopCopy.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/parts/CurrentStopCard.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/parts/DeliveryCompletionSummary.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/parts/DeliveryProofSection.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/parts/RouteProgressHeader.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/parts/RouteProgressTrack.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/parts/RouteScanList.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/parts/StopActionButton.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/parts/StopLocationCard.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/parts/StopOrderList.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/parts/StopVerificationActionBar.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/parts/StopVerificationHeader.tsx` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/persistStopDeliveryProof.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/pickStopProofImage.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/resolveDriverJobExecutionMode.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/sesStopsFromCommerceMission.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/stopVerificationSummary.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/job-card/tripCompletionSummary.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/services/dco.service.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/services/driverFleetOwner.service.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/services/driverGeneralExpense.util.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/services/fleetOwnerCapacityStory.service.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/services/fleetOwnerLoads.service.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/services/marketBids.service.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/services/ownerVehicleDocuments.service.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/services/ownerVehicles.service.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/tripHistory/tripHistoryDetail.styles.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/tripSettlement/driverTripSettlement.util.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/utils/calculateNextPingInterval.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/utils/driverActiveOpsTrip.util.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/utils/driverAlertGuidance.util.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/utils/driverCommunication.util.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/utils/driverDashboardFlags.util.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/utils/driverGpayTransactions.util.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/utils/driverPaymentRequestPreviewHtml.util.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/utils/driverTripSequence.util.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/utils/driverTripStatusNotes.util.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/utils/long_haul_heartbeat.util.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/utils/missionStagePeekLabel.util.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/utils/ownerVehicleDocuments.util.ts` | S1 | driver seed, no live main-app importer |
| `features/driver/utils/pendingEarningsSections.util.ts` | S1 | driver seed, no live main-app importer |
| `features/drivers/components/DriverSelfLedgerPanel.tsx` | N1 | only reachable from driver code |
| `features/drivers/components/DriverWalletEarningsPanel.tsx` | N1 | only reachable from driver code |
| `features/drivers/hooks/useDriverLocation.ts` | N1 | only reachable from driver code |
| `features/drivers/hooks/useHoldButton.ts` | N1 | only reachable from driver code |
| `features/drivers/hooks/useTripControl.ts` | N1 | only reachable from driver code |
| `features/drivers/hooks/useTripDocumentsByType.ts` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverAvailableScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverChatScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverControlScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverDocumentsScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverHomeScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverLevelProgressionScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverNotificationsScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverPassbookDetailScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverPendingEarningsScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverProfileScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverRequestsScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverSalaryRequestDetailScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverSalaryRequestScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverSettingsScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverTripHistoryScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/screens/DriverWalletScreen.tsx` | N1 | only reachable from driver code |
| `features/drivers/services/driverKycDocuments.service.ts` | N1 | only reachable from driver code |
| `features/drivers/services/tripControlProgress.storage.ts` | N1 | only reachable from driver code |
| `features/drivers/utils/resolveDriverOrgAvatar.util.ts` | N1 | only reachable from driver code |
| `features/drivers/utils/salaryRequestDisplay.util.ts` | N1 | only reachable from driver code |
| `features/reach/components/DriverDirectBidSheet.tsx` | N1 | only reachable from driver code |
| `features/reach/components/DriverPulseStoryReel.tsx` | N1 | only reachable from driver code |
| `features/reach/components/DriverReferralEarningsCard.tsx` | N1 | only reachable from driver code |
| `features/reach/components/ShipperCounterHighlight.tsx` | N1 | only reachable from driver code |
| `features/reach/screens/DriverCapacityStoryViewer.tsx` | N1 | only reachable from driver code |
| `features/reach/screens/DriverStoriesScreen.tsx` | N1 | only reachable from driver code |
| `features/reach/utils/driverParticipation.ts` | N1 | only reachable from driver code |
| `features/reach/utils/referralEarnings.ts` | N1 | only reachable from driver code |
| `features/tracking/hooks/useDriverTrackingPublisher.ts` | N1 | only reachable from driver code |
| `features/tracking/session/DriverTrackingSessionManager.ts` | N1 | only reachable from driver code |
| `features/tracking/session/trackingMovementFilter.ts` | N1 | only reachable from driver code |
| `features/tripCompliance/components/LoadingComplianceNotice.tsx` | N1 | only reachable from driver code |
| `features/trips/operations/hub/ExpenseEntryDetailPanel.tsx` | N1 | only reachable from driver code |
| `lib/driverLivePositionBus.ts` | N1 | only reachable from driver code |
| `lib/driverMapRoute.util.ts` | N1 | only reachable from driver code |
| `lib/hooks/useOrgBrandingByIds.ts` | N1 | only reachable from driver code |
| `lib/mapLibreCompat.maplibreImpl.tsx` | N1 | only reachable from driver code |
| `lib/mapLibreCompat.native.tsx` | N1 | only reachable from driver code |
| `lib/mapLibreCompat.rnmapsImpl.tsx` | N1 | only reachable from driver code |
| `lib/mapStyles.ts` | N1 | only reachable from driver code |
| `lib/mapViewRef.types.ts` | N1 | only reachable from driver code |
| `lib/maps/mapLibreCompatImplementation.ts` | N1 | only reachable from driver code |
| `lib/mapsNavigation.util.ts` | N1 | only reachable from driver code |
| `lib/media/compressLocalImage.util.ts` | N1 | only reachable from driver code |
| `lib/orgBrandingFetch.ts` | N1 | only reachable from driver code |
| `lib/preloadDriverChatWarmup.ts` | N1 | only reachable from driver code |
| `lib/queries/useDcoStatusQuery.ts` | N1 | only reachable from driver code |
| `lib/queries/useDriverFleetOwnerQuery.ts` | N1 | only reachable from driver code |
| `lib/queries/useFleetOwnerOpenLoadsQuery.ts` | N1 | only reachable from driver code |
| `lib/queries/useMyCapacityStoriesQuery.ts` | N1 | only reachable from driver code |
| `lib/queries/useMyMarketAwardsQuery.ts` | N1 | only reachable from driver code |
| `lib/queries/useMyMarketBidForIndentQuery.ts` | N1 | only reachable from driver code |
| `lib/queries/useMyMarketBidsQuery.ts` | N1 | only reachable from driver code |
| `lib/queries/useOwnerVehiclesQuery.ts` | N1 | only reachable from driver code |
| `lib/queries/usePilotWorkSummaryQuery.ts` | N1 | only reachable from driver code |
| `lib/reactNativeMapsCompat.native.ts` | N1 | only reachable from driver code |
| `lib/reactNativeMapsCompat.web.tsx` | N1 | only reachable from driver code |
| `lib/safeForegroundPositionWatch.ts` | N1 | only reachable from driver code |
| `lib/useAvatar.ts` | N1 | only reachable from driver code |

## SHARED_CORE (86)

| File | Rule | Reason |
|---|---|---|
| `constants/DriverLevels.ts` | K1 | design/presentation constants |
| `constants/FinanceTxnTypography.ts` | K1 | design/presentation constants |
| `constants/Layout.ts` | K1 | design/presentation constants |
| `constants/PulsePillButtonChrome.ts` | K1 | design/presentation constants |
| `constants/Theme.ts` | K1 | design/presentation constants |
| `constants/Typography.ts` | K1 | design/presentation constants |
| `constants/UserAvatars.ts` | K1 | design/presentation constants |
| `constants/presetAvatar.ts` | K1 | design/presentation constants |
| `contexts/DriverAvatarContext.tsx` | MANUAL:D4 | next to lib/avatarUpload |
| `contexts/LanguageContext.tsx` | C2 | session/network/language infrastructure (auth split per plan Phase 2) |
| `contexts/NetworkContext.tsx` | C2 | session/network/language infrastructure (auth split per plan Phase 2) |
| `features/trips/utils/mapRouteViewport.util.ts` | MANUAL:D5 | pure viewport math, maps infrastructure |
| `hooks/useFailedImageUriGuard.ts` | L4 | generic infrastructure/helper |
| `lib/appAlert.ts` | L4 | generic infrastructure/helper |
| `lib/authEngine.ts` | L4 | generic infrastructure/helper |
| `lib/brand/pulseBrandMark.tokens.ts` | L4 | generic infrastructure/helper |
| `lib/cache/cacheKeys.ts` | L4 | generic infrastructure/helper |
| `lib/cache/cacheMetadataStore.ts` | L4 | generic infrastructure/helper |
| `lib/cache/cacheMetrics.ts` | L4 | generic infrastructure/helper |
| `lib/cache/domainSync.ts` | L4 | generic infrastructure/helper |
| `lib/cache/mergeDelta.ts` | L4 | generic infrastructure/helper |
| `lib/cache/singleflight.ts` | L4 | generic infrastructure/helper |
| `lib/chatPerf.ts` | MANUAL:D12 | client-side perf counters, infrastructure |
| `lib/color.ts` | L4←pulled | generic infrastructure/helper; pulled into shared by components/driver/LeafletMap.rnmaps.tsx |
| `lib/crashReporter.ts` | L4 | generic infrastructure/helper |
| `lib/emailValidation.ts` | L4 | generic infrastructure/helper |
| `lib/expoGoMaps.ts` | L4 | generic infrastructure/helper |
| `lib/format.ts` | L4 | generic infrastructure/helper |
| `lib/formatEstimatedDuration.ts` | L4 | generic infrastructure/helper |
| `lib/globalId.ts` | L4 | generic infrastructure/helper |
| `lib/hooks/appQueryGateState.ts` | L4 | generic infrastructure/helper |
| `lib/hooks/useAppStateIsActive.ts` | L4 | generic infrastructure/helper |
| `lib/hooks/useKeyboardVisible.ts` | L4 | generic infrastructure/helper |
| `lib/hooks/useLoadingStuck.ts` | L4 | generic infrastructure/helper |
| `lib/hooks/useMirrorIndicator.ts` | L4 | generic infrastructure/helper |
| `lib/hooks/useMobileWebStepLayout.ts` | L4 | generic infrastructure/helper |
| `lib/hooks/useViewportWidth.ts` | L4 | generic infrastructure/helper |
| `lib/i18n.ts` | L4 | generic infrastructure/helper |
| `lib/indexBootRedirect.util.ts` | L4 | generic infrastructure/helper |
| `lib/keepSignedInPreference.ts` | L4 | generic infrastructure/helper |
| `lib/logger.ts` | L4 | generic infrastructure/helper |
| `lib/maps/mapEnvironment.ts` | L4 | generic infrastructure/helper |
| `lib/maps/mapImplementationRegistry.ts` | L4←pulled | generic infrastructure/helper; pulled into shared by lib/maps/leafletNativeImplementation.ts |
| `lib/navigationPolicy/predicateSignals.ts` | L4 | generic infrastructure/helper |
| `lib/pagination.ts` | L4 | generic infrastructure/helper |
| `lib/phoneValidation.ts` | L4 | generic infrastructure/helper |
| `lib/placeCityState.util.ts` | L4 | generic infrastructure/helper |
| `lib/platform/db/platformDb.ts` | MANUAL:D2 | request moderation / scalability / DB wiring = infrastructure |
| `lib/platform/moderator/index.ts` | MANUAL:D2 | request moderation / scalability / DB wiring = infrastructure |
| `lib/platform/moderator/invalidationScheduler.ts` | MANUAL:D2 | request moderation / scalability / DB wiring = infrastructure |
| `lib/platform/moderator/moderatedFetch.ts` | MANUAL:D2 | request moderation / scalability / DB wiring = infrastructure |
| `lib/platform/moderator/requestClassifier.ts` | MANUAL:D2 | request moderation / scalability / DB wiring = infrastructure |
| `lib/platform/moderator/requestModerator.ts` | MANUAL:D2 | request moderation / scalability / DB wiring = infrastructure |
| `lib/platform/moderator/types.ts` | MANUAL:D2 | request moderation / scalability / DB wiring = infrastructure |
| `lib/platform/scalability/index.ts` | MANUAL:D2 | request moderation / scalability / DB wiring = infrastructure |
| `lib/platform/scalability/performanceBudgets.ts` | MANUAL:D2 | request moderation / scalability / DB wiring = infrastructure |
| `lib/platform/scalability/platformHealth.ts` | MANUAL:D2 | request moderation / scalability / DB wiring = infrastructure |
| `lib/platform/scalability/queryCacheMetrics.ts` | MANUAL:D2 | request moderation / scalability / DB wiring = infrastructure |
| `lib/platformShadow.ts` | L4 | generic infrastructure/helper |
| `lib/platformViewStyle.util.ts` | L4 | generic infrastructure/helper |
| `lib/pod/imageCompression.ts` | L4 | generic infrastructure/helper |
| `lib/pulseInputChrome.ts` | L4 | generic infrastructure/helper |
| `lib/pulseMascotIllustrations.ts` | L4 | generic infrastructure/helper |
| `lib/queryClient.ts` | L4 | generic infrastructure/helper |
| `lib/queryRetry.ts` | L4 | generic infrastructure/helper |
| `lib/realtimeRegistry.ts` | L4 | generic infrastructure/helper |
| `lib/routeStackOptions.ts` | L4 | generic infrastructure/helper |
| `lib/routes.ts` | L4 | generic infrastructure/helper |
| `lib/routingService.ts` | L4 | generic infrastructure/helper |
| `lib/scrollFocusedFieldIntoView.util.ts` | L4 | generic infrastructure/helper |
| `lib/significantAppResume.ts` | L4 | generic infrastructure/helper |
| `lib/startupMetrics.ts` | L4 | generic infrastructure/helper |
| `lib/storageSignedUrlCache.ts` | L4 | generic infrastructure/helper |
| `lib/suite/suiteAuthContent.ts` | L4 | generic infrastructure/helper |
| `lib/supabase.ts` | L4 | generic infrastructure/helper |
| `lib/supabaseAbort.util.ts` | L4 | generic infrastructure/helper |
| `lib/supabaseHttp.util.ts` | L4 | generic infrastructure/helper |
| `lib/todayPendingInviteCount.ts` | L4 | generic infrastructure/helper |
| `lib/trackingLocation.constants.ts` | L4 | generic infrastructure/helper |
| `lib/usePreventScreenCapture.ts` | L4 | generic infrastructure/helper |
| `lib/useSafeBack.ts` | L4 | generic infrastructure/helper |
| `lib/utils/lr.ts` | L4 | generic infrastructure/helper |
| `lib/uuidv7.ts` | L4 | generic infrastructure/helper |
| `lib/webKeyboard.ts` | L4 | generic infrastructure/helper |
| `lib/webOverlayPortal.tsx` | L4 | generic infrastructure/helper |
| `lib/webViewportHeight.ts` | L4 | generic infrastructure/helper |

## SHARED_DOMAIN (275)

| File | Rule | Reason |
|---|---|---|
| `contexts/ActiveWorkspaceContext.tsx` | MANUAL:D3 | shared context (D3 amended by D11: depends on AuthContext, which is domain for now); driver app supplies its own DRIVER_ADAPTER provider; context logic unchanged |
| `contexts/AuthContext.tsx` | MANUAL:D11 | shared business code for now; session part moves to @pulse/core in the Phase 2 auth split; no behavior change |
| `contexts/OrganizationContext.tsx` | MANUAL:D3 | shared context (D3 amended by D11: depends on AuthContext, which is domain for now); driver app supplies its own DRIVER_ADAPTER provider; context logic unchanged |
| `features/auth/hooks/useMobileKeepSignedInSignOut.ts` | F2 | feature service/util/hook/domain code |
| `features/auth/hooks/useWebKeepSignedInSignOut.ts` | F2 | feature service/util/hook/domain code |
| `features/auth/services/auth.service.ts` | F2 | feature service/util/hook/domain code |
| `features/auth/signup/signUpConstants.ts` | F2 | feature service/util/hook/domain code |
| `features/auth/signup/signUpDriverTheme.ts` | F2 | feature service/util/hook/domain code |
| `features/auth/signup/signUpKeypad.util.ts` | F2 | feature service/util/hook/domain code |
| `features/auth/signup/signUpMobile.styles.ts` | F2 | feature service/util/hook/domain code |
| `features/auth/signup/signUpMobileTokens.ts` | F2 | feature service/util/hook/domain code |
| `features/auth/signup/signUpPulseTheme.ts` | F2 | feature service/util/hook/domain code |
| `features/auth/signup/signUpTypography.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/chatMobileLayout.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/chatTheme.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/components/desktop/chatSlackDesktop.styles.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/components/mobile/chatSlackMobile.styles.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/components/shared/chatDriverSwapAvatar.styles.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/components/shared/chatSlackAvatar.constants.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/components/shared/chatSlackStreamTabs.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/persist/chatBootstrapPersist.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/realtime/activeTripMessageScope.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/services/chat.service.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/services/chatAssignmentBridge.service.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/services/chatLedgerBridge.service.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/services/networkConversation.service.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/store/useChatStore.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/types/chat.types.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/assignmentAuditChatMessages.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/chatAvatar.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/chatEmojiAnim.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/chatMediaBurst.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/chatMediaGallery.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/chatMessageMarkdown.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/chatPreviewTransform.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/conversationImagePreview.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/dedupeTripStatusBroadcastForLane.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/documentShareDisplay.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/eventPayloadMerge.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/feedbackRequestMeta.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/feedbackRequestMeta.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/fitImageInViewport.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/laneMultiplexer.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/ledgerVisibility.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/locationLogPayload.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/locationPingChatDisplay.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/longHaulChat.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/mergeTripFeedbackMessages.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/messagePartyVisibility.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/missionDebrief.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/partyDisplay.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/resolveChatDocumentUrl.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/slackMessageGroup.util.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/storageRenderImageUrl.ts` | F2 | feature service/util/hook/domain code |
| `features/chat/utils/thinImageMetadata.ts` | F2 | feature service/util/hook/domain code |
| `features/clients/services/clients.service.ts` | F2 | feature service/util/hook/domain code |
| `features/connections/services/connectionRequests.service.ts` | F2 | feature service/util/hook/domain code |
| `features/driver/job-card/deliveryProof.ts` | MANUAL:D4 | used by tripCompliance |
| `features/driver/services/driverLocation.service.ts` | MANUAL:D4 | used by trip detail + track & trace |
| `features/driver/tripHistory/tripHistoryDetail.util.ts` | MANUAL:D4 | used by TripDetailScreen |
| `features/drivers/services/drivers.service.ts` | F2 | feature service/util/hook/domain code |
| `features/drivers/services/salaryRequests.service.ts` | F2 | feature service/util/hook/domain code |
| `features/drivers/utils/driverInviteCompensation.util.ts` | F2 | feature service/util/hook/domain code |
| `features/drivers/utils/driverInviteOffer.util.ts` | F2 | feature service/util/hook/domain code |
| `features/drivers/utils/driverUtils.util.ts` | F2 | feature service/util/hook/domain code |
| `features/experience/experienceProgress.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/accounting/accountingModel.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/aggregation/aggregateDrivers.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/domain/financeCounterpartyLane.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/ledger/ledgerEntryModel.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/mappers/tripCostEventMappers.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/projections/deriveOperationalCashflow.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/projections/deriveOperationalLedgerProjection.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/projections/deriveOperationalPayables.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/projections/derivePendingReimbursements.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/projections/index.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/projections/syncOperationalFinanceProjection.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/selectors/assetTripProvisionSelectors.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/selectors/integritySelectors.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/selectors/tripAccountingSelectors.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/services/finance.service.ts` | F2 | feature service/util/hook/domain code |
| `features/finance/utils/ledgerWriteCompat.util.ts` | F2 | feature service/util/hook/domain code |
| `features/indents/constants.ts` | F2 | feature service/util/hook/domain code |
| `features/ledger/vehicle/index.ts` | F2 | feature service/util/hook/domain code |
| `features/ledger/vehicle/postVehicleOperationalEntry.ts` | F2 | feature service/util/hook/domain code |
| `features/ledger/vehicle/postingMessages.util.ts` | F2 | feature service/util/hook/domain code |
| `features/ledger/vehicle/postingSelectors.ts` | F2 | feature service/util/hook/domain code |
| `features/ledger/vehicle/reconciliation/index.ts` | F2 | feature service/util/hook/domain code |
| `features/ledger/vehicle/reconciliation/reconciliation.service.ts` | F2 | feature service/util/hook/domain code |
| `features/ledger/vehicle/reconciliation/usePostingReconciliation.ts` | F2 | feature service/util/hook/domain code |
| `features/ledger/vehicle/runtime/index.ts` | F2 | feature service/util/hook/domain code |
| `features/ledger/vehicle/runtime/postingRuntime.service.ts` | F2 | feature service/util/hook/domain code |
| `features/ledger/vehicle/syncOperationalExpenseToCashLedger.service.ts` | F2 | feature service/util/hook/domain code |
| `features/ledger/vehicle/vehicleEconomics.ts` | F2 | feature service/util/hook/domain code |
| `features/ledger/vehicle/vehicleLedgerEngine.ts` | F2 | feature service/util/hook/domain code |
| `features/ledger/vehicle/vehiclePostingRules.ts` | F2 | feature service/util/hook/domain code |
| `features/marketplace/utils/fleetFit.util.ts` | F2 | feature service/util/hook/domain code |
| `features/marketplace/utils/marketplaceErrorFormat.util.ts` | F2 | feature service/util/hook/domain code |
| `features/network/services/marketBids.service.ts` | F2 | feature service/util/hook/domain code |
| `features/network/services/posts.service.ts` | F2 | feature service/util/hook/domain code |
| `features/network/utils/storyDisplay.ts` | F2 | feature service/util/hook/domain code |
| `features/ocr/services/ocrJob.service.ts` | F2 | feature service/util/hook/domain code |
| `features/onboarding/components/onboardingPersonaAssets.ts` | F2 | feature service/util/hook/domain code |
| `features/operations/display/index.ts` | F2 | feature service/util/hook/domain code |
| `features/operations/display/operationalDisplay.ts` | F2 | feature service/util/hook/domain code |
| `features/operations/numbering/allocateOperationalSequence.ts` | F2 | feature service/util/hook/domain code |
| `features/operations/numbering/generateOperationalCode.ts` | F2 | feature service/util/hook/domain code |
| `features/operations/numbering/index.ts` | F2 | feature service/util/hook/domain code |
| `features/operations/numbering/operationalNumbering.service.ts` | F2 | feature service/util/hook/domain code |
| `features/operations/numbering/sequenceSelectors.ts` | F2 | feature service/util/hook/domain code |
| `features/organization/services/organization.service.ts` | F2 | feature service/util/hook/domain code |
| `features/organization/types/organizationKycDocuments.types.ts` | F2 | feature service/util/hook/domain code |
| `features/organization/utils/kycVerification.util.ts` | F2 | feature service/util/hook/domain code |
| `features/organization/utils/teamInviteRoles.util.ts` | F2←pulled | feature service/util/hook/domain code; pulled into shared by contexts/ActiveWorkspaceContext.tsx |
| `features/ratings/services/ratings.service.ts` | F2 | feature service/util/hook/domain code |
| `features/reach/services/analytics.service.ts` | F2 | feature service/util/hook/domain code |
| `features/reach/services/campaigns.service.ts` | F2 | feature service/util/hook/domain code |
| `features/reach/services/driverReferrals.service.ts` | F2 | feature service/util/hook/domain code |
| `features/reach/utils/directBidLifecycle.ts` | F2 | feature service/util/hook/domain code |
| `features/tracking/broadcast/publishTrackingBroadcast.ts` | F2 | feature service/util/hook/domain code |
| `features/tracking/broadcast/trackingBroadcastChannels.ts` | F2 | feature service/util/hook/domain code |
| `features/tracking/constants.ts` | F2 | feature service/util/hook/domain code |
| `features/tracking/map/TripTrackingMapStore.ts` | F2 | feature service/util/hook/domain code |
| `features/tracking/services/driverPresence.service.ts` | F2 | feature service/util/hook/domain code |
| `features/tracking/services/trackingCheckpoint.service.ts` | F2 | feature service/util/hook/domain code |
| `features/tracking/trackingFeatureFlags.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/capabilities/capabilityEngine.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/capabilities/index.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/capabilities/operationalModels.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/capabilities/permissions.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/capabilities/selectors.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/components/add-trip/createTripDesktop.styles.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/components/trip-detail/tripDocTypes.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/domain/index.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/domain/tripAlertActions.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/domain/tripDcoOperating.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/domain/tripExecutionModel.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/domain/tripJourneyMetrics.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/domain/tripOperationalAlerts.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/domain/tripStage.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/domain/tripStageEta.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/domain/tripStageGuidance.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/domain/tripStageMetadata.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/domain/tripStageMetrics.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/domain/tripTimeline.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/fuel/fuel.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/hooks/useTripOperationsSync.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/maintenance/maintenance.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/mileage/mileageEngine.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/offline/outbox.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/offline/sync.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/other/otherExpense.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/queries/useTripOperations.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/reimbursement/driverExpenseRequest.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/reimbursement/reimbursement.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/reimbursement/reimbursementState.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/shared/driverExpenseCategoryNav.util.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/shared/driverReimbursementEvents.util.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/shared/expenseEntryEdit.util.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/shared/expensePreview.util.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/shared/operationsEntryOptions.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/shared/tripOtherExpenseCategories.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/timeline/timelineEvents.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/toll/toll.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/uploads/photoUploads.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/operations/vehicle/vehicleOperationsLedger.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/services/ewayBillFields.util.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/services/lrDocumentOcr.util.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/services/trip-assignment-audit.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/services/tripDocumentLrPod.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/services/tripDocuments.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/services/tripOtp.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/services/tripWorkflow.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/services/trips.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/utils/applyIndentCommerceOrigin.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/utils/driverAssignerDisplay.util.ts` | F2←pulled | feature service/util/hook/domain code; pulled into shared by features/driver/tripHistory/tripHistoryDetail.util.ts |
| `features/trips/utils/driverLastPingDisplay.util.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/utils/formatTrackingTimestamp.util.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/utils/tripOrgFetch.util.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/utils/tripPreservableStatuses.util.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/utils/tripReassign.util.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/utils/tripReassignConflict.util.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/utils/tripTrackingStatus.util.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/verification/GPSDistanceHook.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/verification/hooks/useTripVerificationSync.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/verification/offline/outbox.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/verification/offline/sync.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/verification/queries/useTripVerification.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/verification/selectors/verificationSelectors.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/verification/state/verificationState.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/verification/uploads/odometerUploads.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/verification/verification.service.ts` | F2 | feature service/util/hook/domain code |
| `features/trips/visibility/tripVisibility.ts` | F2 | feature service/util/hook/domain code |
| `features/vehicles/services/vehicles.service.ts` | F2 | feature service/util/hook/domain code |
| `features/vehicles/utils/fleetAvatar.util.ts` | F2 | feature service/util/hook/domain code |
| `features/vehicles/utils/indianTruckData.util.ts` | F2 | feature service/util/hook/domain code |
| `features/vehicles/utils/vehicleFormOptions.util.ts` | F2 | feature service/util/hook/domain code |
| `lib/avatarContext.ts` | L4→V1 | generic infrastructure/helper; promoted: depends on domain lib/avatarUpload.ts |
| `lib/avatarUpload.ts` | L4→V1 | generic infrastructure/helper; promoted: depends on domain contexts/AuthContext.tsx |
| `lib/driverInviteDeepLink.util.ts` | L3 | business-named shared module |
| `lib/driverPerfMetrics.ts` | L3 | business-named shared module |
| `lib/enrichConnectionPartnerAvatars.ts` | L3 | business-named shared module |
| `lib/firstLaunch.ts` | L3 | business-named shared module |
| `lib/globalSync/inboundProtocol.util.ts` | L3 | business-named shared module |
| `lib/globalSync/priorityEngine.util.ts` | L3 | business-named shared module |
| `lib/globalSync/registryFeed.constants.ts` | L3 | business-named shared module |
| `lib/globalSync/useGlobalSyncStore.ts` | L3 | business-named shared module |
| `lib/idempotencyService.ts` | L3 | business-named shared module |
| `lib/indianVehicleInput.util.ts` | L3 | business-named shared module |
| `lib/ledgerBookPendingStore.ts` | L3 | business-named shared module |
| `lib/mapLocationLabel.service.ts` | L3 | business-named shared module |
| `lib/memberSurfaces.ts` | L3←pulled | business-named shared module; pulled into shared by features/organization/utils/teamInviteRoles.util.ts |
| `lib/onboarding/businessSignupBranding.util.ts` | L3 | business-named shared module |
| `lib/onboarding/incompleteOwnerOrg.util.ts` | L3 | business-named shared module |
| `lib/onboarding/membershipTypes.ts` | L3←pulled | business-named shared module; pulled into shared by lib/platform-identity/types/workspace.ts |
| `lib/onboarding/onboardingTypes.ts` | L3 | business-named shared module |
| `lib/onboarding/useSignupKeypadInput.ts` | L3 | business-named shared module |
| `lib/onboarding/workspaceSetupContent.ts` | L3 | business-named shared module |
| `lib/partyAvatarDisplay.ts` | L3 | business-named shared module |
| `lib/partyOfflineRoleAvatar.ts` | L3 | business-named shared module |
| `lib/phoneLookup.ts` | L3 | business-named shared module |
| `lib/placesService.ts` | L3 | business-named shared module |
| `lib/platform-identity/types/workspace.ts` | MANUAL:D2 | platform identity/workspace |
| `lib/platform-identity/workspace/workspaceContextStore.ts` | MANUAL:D2 | platform identity/workspace |
| `lib/platform/events/EventBus.contract.ts` | MANUAL:D2 | platform business services |
| `lib/platform/events/InProcessEventBus.ts` | MANUAL:D2 | platform business services |
| `lib/platform/events/PlatformEventLog.ts` | MANUAL:D2 | platform business services |
| `lib/platform/events/commands.ts` | MANUAL:D2 | platform business services |
| `lib/platform/events/types.ts` | MANUAL:D2 | platform business services |
| `lib/platform/index.ts` | MANUAL:D2 | temporary compatibility shim (oms @pulse-platform untouched) |
| `lib/platform/mappers/clientHubProfileMapper.ts` | MANUAL:D2 | platform business services |
| `lib/platform/mappers/clientMappers.ts` | MANUAL:D2 | platform business services |
| `lib/platform/mappers/warehouseMappers.ts` | MANUAL:D2 | platform business services |
| `lib/platform/orchestration/ExecutionOrchestrator.contract.ts` | MANUAL:D2 | platform business services |
| `lib/platform/orchestration/ExecutionOrchestrator.ts` | MANUAL:D2 | platform business services |
| `lib/platform/orchestration/summarizeStopLocations.ts` | MANUAL:D2 | platform business services |
| `lib/platform/orchestration/types.ts` | MANUAL:D2 | platform business services |
| `lib/platform/orchestration/verifyPublishAcceptance.ts` | MANUAL:D2 | platform business services |
| `lib/platform/repositories/customerRepository.ts` | MANUAL:D2 | platform business services |
| `lib/platform/repositories/executionPlanRepository.ts` | MANUAL:D2 | platform business services |
| `lib/platform/repositories/indentRepository.ts` | MANUAL:D2 | platform business services |
| `lib/platform/repositories/orderRepository.ts` | MANUAL:D2 | platform business services |
| `lib/platform/repositories/productRepository.ts` | MANUAL:D2 | platform business services |
| `lib/platform/repositories/userRepository.ts` | MANUAL:D2 | platform business services |
| `lib/platform/repositories/warehouseRepository.ts` | MANUAL:D2 | platform business services |
| `lib/platform/services/CustomerService.ts` | MANUAL:D2 | platform business services |
| `lib/platform/services/ExecutionPlanService.ts` | MANUAL:D2 | platform business services |
| `lib/platform/services/IndentService.ts` | MANUAL:D2 | platform business services |
| `lib/platform/services/OrderService.ts` | MANUAL:D2 | platform business services |
| `lib/platform/services/PlatformReadinessService.ts` | MANUAL:D2 | platform business services |
| `lib/platform/services/ProductService.ts` | MANUAL:D2 | platform business services |
| `lib/platform/services/WarehouseService.ts` | MANUAL:D2 | platform business services |
| `lib/queries/linkedOrgDisplayCache.ts` | Q1 | query hooks / domain query keys |
| `lib/queries/operationalInvalidation.ts` | Q1 | query hooks / domain query keys |
| `lib/queries/useDriverAvailabilityQuery.ts` | Q1 | query hooks / domain query keys |
| `lib/queries/useDriverHomeDriversQuery.ts` | Q1 | query hooks / domain query keys |
| `lib/queries/useDriverInvitesQuery.ts` | Q1 | query hooks / domain query keys |
| `lib/queries/useDriverUiTripsQuery.ts` | Q1 | query hooks / domain query keys |
| `lib/queries/useInvalidateDriverHomeDashboard.ts` | Q1 | query hooks / domain query keys |
| `lib/queries/usePendingOtpTripsQuery.ts` | Q1 | query hooks / domain query keys |
| `lib/queries/useReachCampaignsQuery.ts` | Q1 | query hooks / domain query keys |
| `lib/queries/useTripCheckpointDistanceQuery.ts` | Q1 | query hooks / domain query keys |
| `lib/queries/useTripDriverPresenceQuery.ts` | Q1 | query hooks / domain query keys |
| `lib/queries/useTripTimelineQuery.ts` | Q1 | query hooks / domain query keys |
| `lib/queries/useWorkspaceProductsQuery.ts` | Q1 | query hooks / domain query keys |
| `lib/queryKeys.ts` | Q1 | query hooks / domain query keys |
| `lib/reverseGeocodePlace.util.ts` | L4→V1 | generic infrastructure/helper; promoted: depends on domain lib/mapLocationLabel.service.ts |
| `lib/sameOrgPartyGuard.ts` | L3 | business-named shared module |
| `lib/signupMobileWebFormScroll.ts` | L3 | business-named shared module |
| `lib/signupPasswordInput.util.ts` | L3 | business-named shared module |
| `lib/syncLinkedDriversForDriverHome.ts` | L3 | business-named shared module |
| `lib/tripChatInvalidate.ts` | L3 | business-named shared module |
| `lib/validation.ts` | L4→V1 | generic infrastructure/helper; promoted: depends on domain lib/indianVehicleInput.util.ts |
| `types/organization.ts` | T1 | shared domain types |
| `types/trip-views.ts` | T1 | shared domain types |

## SHARED_UI (45)

| File | Rule | Reason |
|---|---|---|
| `components/AppLoadingSplash.tsx` | U1 | shared component |
| `components/CenteredLoadingView.tsx` | U1 | shared component |
| `components/CreateTripSheetSearchInput.tsx` | U1 | shared component |
| `components/LazySuspenseFallback.tsx` | U1 | shared component |
| `components/LoadingIndicator.tsx` | U1 | shared component |
| `components/NativeHtmlWebView.tsx` | U1 | shared component |
| `components/PulseMascotBanner.tsx` | U1 | shared component |
| `components/PulsePillButton.tsx` | U1 | shared component |
| `components/RouteLoadingScreen.tsx` | U1 | shared component |
| `components/SearchBar.tsx` | U1 | shared component |
| `components/ThemedAlertModal.tsx` | U1 | shared component |
| `components/ThemedConfirmModal.tsx` | U1 | shared component |
| `components/TinyEmptyLottie.tsx` | U1 | shared component |
| `components/TripFeedbackModal.tsx` | U1 | shared component |
| `components/brand/PulseBrandMark.tsx` | U1 | shared component |
| `components/driver/DriverMapAvatarMarker.tsx` | MANUAL:D8 | used by shared LeafletMap family |
| `components/driver/LeafletMap.expoWebView.tsx` | S2 | LeafletMap family is used by the dispatcher map (plan §1) |
| `components/driver/LeafletMap.maplibre.tsx` | S2 | LeafletMap family is used by the dispatcher map (plan §1) |
| `components/driver/LeafletMap.rnmaps.tsx` | S2 | LeafletMap family is used by the dispatcher map (plan §1) |
| `components/driver/LeafletMap.tsx` | S2 | LeafletMap family is used by the dispatcher map (plan §1) |
| `components/driver/LeafletMap.types.ts` | S2 | LeafletMap family is used by the dispatcher map (plan §1) |
| `components/driver/LeafletMap.web.tsx` | S2 | LeafletMap family is used by the dispatcher map (plan §1) |
| `components/driver/LeafletMapZoomControls.tsx` | S2 | LeafletMap family is used by the dispatcher map (plan §1) |
| `components/hub/HubPromoLottie.tsx` | U1 | shared component |
| `components/mobile-input/BidVsTargetHint.tsx` | U1 | shared component |
| `components/mobile-input/DecimalKeypad.tsx` | U1 | shared component |
| `components/mobile-input/NumericDisplay.tsx` | U1 | shared component |
| `components/mobile-input/SmartInputTrigger.tsx` | U1 | shared component |
| `components/mobile-input/bidVsTarget.ts` | U1 | shared component |
| `components/mobile-input/feedback.ts` | U1 | shared component |
| `components/mobile-input/formatters.ts` | U1 | shared component |
| `components/mobile-input/keypad.ts` | U1 | shared component |
| `components/mobile-input/useInputPlatform.ts` | U1 | shared component |
| `components/mobile-input/usePhysicalKeypadInput.ts` | U1 | shared component |
| `components/mobile-input/validation.ts` | U1 | shared component |
| `components/party/keypad/KeypadDisplayValueWithCaret.tsx` | U1 | shared component |
| `contexts/DriverThemeContext.tsx` | MANUAL:D4 | theme used by trips/operations/shared |
| `features/auth/signup/SignUpPulseFormStepContext.tsx` | MANUAL:D1 | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain |
| `features/auth/signup/components/DriverSignupReadyCard.tsx` | MANUAL:D1 | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain |
| `features/driver/job-card/parts/RoutePlanMapPin.tsx` | MANUAL:D14 | rendered by the shared LeafletMap family |
| `lib/chatPaymentLottieAssets.ts` | L1 | asset/lottie module |
| `lib/mapMarkerIcons.util.ts` | L2 | renders UI |
| `lib/maps/leafletNativeImplementation.ts` | MANUAL:D8 | component loader, not infrastructure |
| `lib/reportWatermark.util.ts` | L2 | renders UI |
| `lib/signup/signUpMarketingAssets.ts` | L1 | asset/lottie module |

## SHARED_FEATURES (64)

| File | Rule | Reason |
|---|---|---|
| `components/Avatar.tsx` | MANUAL:D13 | renders party/business avatars (needs avatar data from domain) |
| `components/PartyAvatar.tsx` | MANUAL:D13 | renders party/business avatars (needs avatar data from domain) |
| `components/mobile-input/FullscreenNumericEntry.tsx` | U1→D13 | shared component; D13: renders components/mobile-input/NumericEntryRecipientHero.tsx |
| `components/mobile-input/NumericEntryRecipientHero.tsx` | U1→D13 | shared component; D13: renders components/PartyAvatar.tsx |
| `components/mobile-input/SmartInput.tsx` | U1→D13 | shared component; D13: renders components/mobile-input/FullscreenNumericEntry.tsx |
| `features/auth/components/GoogleBrandIcon.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/auth/signup/SignUpMobileShell.tsx` | MANUAL:D1→fallback | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain; fell back to SHARED_FEATURES: needs SHARED_DOMAIN lib/signupMobileWebFormScroll.ts |
| `features/auth/signup/SignUpOtpBoxes.tsx` | MANUAL:D1→fallback | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain; fell back to SHARED_FEATURES: needs SHARED_DOMAIN features/auth/signup/signUpConstants.ts |
| `features/auth/signup/SignUpPulseField.tsx` | MANUAL:D1→fallback | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain; fell back to SHARED_FEATURES: needs SHARED_DOMAIN lib/signupPasswordInput.util.ts |
| `features/auth/signup/SignUpPulseFormStep.tsx` | MANUAL:D1→fallback | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain; fell back to SHARED_FEATURES: needs SHARED_DOMAIN lib/signupMobileWebFormScroll.ts |
| `features/auth/signup/SignUpPulseKeypadStep.tsx` | MANUAL:D1→fallback | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain; fell back to SHARED_FEATURES: needs SHARED_FEATURES features/auth/components/GoogleBrandIcon.tsx |
| `features/auth/signup/SignUpPulsePrimaryButton.tsx` | MANUAL:D1→fallback | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain; fell back to SHARED_FEATURES: needs SHARED_DOMAIN features/auth/signup/signUpConstants.ts |
| `features/auth/signup/SignUpPulseShell.tsx` | MANUAL:D1→fallback | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain; fell back to SHARED_FEATURES: needs SHARED_DOMAIN features/auth/signup/signUpDriverTheme.ts |
| `features/auth/signup/SignUpPulseTitle.tsx` | MANUAL:D1→fallback | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain; fell back to SHARED_FEATURES: needs SHARED_DOMAIN features/auth/signup/signUpConstants.ts |
| `features/auth/signup/components/PulseActivationDesktopSplit.tsx` | MANUAL:D1→fallback | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain; fell back to SHARED_FEATURES: needs SHARED_DOMAIN features/auth/signup/signUpConstants.ts |
| `features/auth/signup/components/PulseActivationMarketingPanel.tsx` | MANUAL:D1→fallback | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain; fell back to SHARED_FEATURES: needs SHARED_FEATURES features/onboarding/components/WorkspaceOutcomeTypewriter.tsx |
| `features/auth/signup/components/SignUpBrandingStepLayout.tsx` | MANUAL:D1→fallback | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain; fell back to SHARED_FEATURES: needs SHARED_DOMAIN lib/signupMobileWebFormScroll.ts |
| `features/auth/signup/components/SignUpPhotoPickerStep.tsx` | MANUAL:D1→fallback | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain; fell back to SHARED_FEATURES: needs SHARED_FEATURES features/auth/signup/components/SignUpBrandingStepLayout.tsx |
| `features/auth/signup/steps/DriverSignupSuccessStep.tsx` | MANUAL:D1→fallback | signup shell/keypad UI (rendering files only; non-rendering signup files follow the normal rules); @pulse/features if runtime deps need domain; fell back to SHARED_FEATURES: needs SHARED_DOMAIN lib/onboarding/businessSignupBranding.util.ts |
| `features/chat/components/ChatDocumentThreadPreview.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/ChatEventCard.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/ChatImage.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/ChatLocationSystemCard.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/ChatMobileComposer.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/ChatPartyAvatar.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/ChatPaymentEventIcon.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/DocumentPreviewModal.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/DocumentShareCard.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/SmartChatImage.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/mobile/ChatSlackMessageRow.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/mobile/ChatSlackMobileChrome.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatAnimatedEmoji.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatComposerMarkdownInput.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatDriverSwapAvatar.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatDriverSwapAvatarCompact.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatDriverSwapInboxPreview.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatDriverSwapPreviewCopy.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatHistoryExpiryNotice.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatInboxImagePreviewStrip.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatJumboEmojiMessage.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatListPreviewText.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatLocationPingInboxPreview.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatMediaBurstRow.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatMediaGalleryLightbox.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatMessageContextMenu.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatReactionsRow.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatReplyPreview.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatSlackDocumentAttachment.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatSlackMirrorToggle.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/components/shared/ChatThreadMediaStrip.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/chat/utils/chatInlineMarkdown.util.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/driver/components/RazorpayTestPreviewSheet.tsx` | MANUAL:D4 | used by network/OrgMyBidsList |
| `features/experience/components/MilestoneHowToModal.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/marketplace/components/PilotPaymentMethodSheet.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/marketplace/components/RazorpayCheckoutSheet.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/network/components/RouteEndpointStack.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/network/components/StoryBroadcastPreview.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/network/components/bidding/BidConfirmModal.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/onboarding/components/PulseSplitBrandLogo.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/onboarding/components/WorkspaceOutcomeTypewriter.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/reach/screens/DriverPulseStoryViewer.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/trips/components/add-trip/LocationSearchField.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/trips/verification/VerificationStatusChip.tsx` | F1 | shared business UI (D1: @pulse/features) |
| `features/trips/verification/components/VerificationStatusChip.tsx` | MANUAL:D15 | one-line re-export; goes with its target |

## MAIN_ONLY (481)

| File | Rule | Reason |
|---|---|---|
| `components/AddTransactionModal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/AlertRegistryCardActions.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/CapacityDialPicker.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ChatFilterMirrorToggle.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/DatePresetPillBar.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/DateRangePickerModal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/EntityAvatar.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/EntityIdentityAvatar.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/FAB.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/FinanceFAB.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/LedgerFlowGuardAlert.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/LiquidFillPill.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/PartyAddChip.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/PartyAddModalChrome.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/PartySpeedDialFab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/PersistentTabPanel.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/SemanticAddIcon.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/TeslaHeader.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/TripFinancialCard.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/TripLedgerFinancialSummary.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/WizardStepLayout.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/chromeLoadingScreens.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/FullPageWizardBlock.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/FullPageWizardFooter.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/FullPageWizardPartyCard.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/FullPageWizardProgress.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/FullPageWizardShell.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/WizardActionBarContext.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/WizardClientPicker.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/WizardContextSummary.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/WizardDesktopFrame.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/WizardEntityPartyCell.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/WizardEntityPicker.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/WizardFormBody.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/WizardInsightRail.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/WizardNumericKeypadFlow.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/WizardPartyContextRow.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/WizardPriorSelections.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/WizardSelectionGrid.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/fullPageWizardStyles.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/full-page-wizard/wizardDesktopInsights.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/indianVehicle/IndianVehicleRegistrationKeypad.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/indianVehicle/IndianVehicleRegistrationKeypadFlow.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/indianVehicle/useIndianVehiclePhysicalKeypad.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/LedgerEntryReceiptCard.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/LedgerFocusedTripCard.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/LedgerFocusedTripSettlementSummary.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/LedgerHeaderTripDetail.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/LedgerMobileWizard.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/LedgerProtocolStrip.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/LedgerReconSummaryCard.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/LedgerReconSummaryModal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/LedgerSettlementPctDock.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/LedgerTripSettlementNote.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/LedgerWebDateField.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/LedgerWebDateField.web.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/ledgerEntryReceiptPalette.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/ledgerPaymentVisuals.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/paymentModeLogos.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/paymentTypeAssets.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/paymentTypeLogos.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/ledger/paymentTypeVisuals.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/mobile-input/FullscreenTextEntry.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/mobile-input/NumericEntryPartyBanner.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/mobile-input/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/mobile-input/types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/navigation/PulseBottomTabBar/dockMetrics.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/operational/MetricDisplay.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/operational/OperationalBottomActionBar.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/operational/OperationalButton.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/operational/OperationalChipSelect.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/operational/OperationalEmptyState.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/operational/OperationalHeader.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/operational/OperationalListRow.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/operational/OperationalSkeleton.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/operational/Surface.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/operational/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/operational/useOperationalDensity.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/party/IndiaFlagIcon.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/party/PartyContactCombinedForm.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/party/PartyContactMobileWizard.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/party/PartyCreateSuccessModal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/party/PartyDriverMobileWizard.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/party/PartyMobileWizardReview.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/party/PartyMobileWizardShell.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/party/PartyVehicleMobileWizard.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/party/keypad/DriverNameKeypadFlow.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/party/keypad/IndianDrivingLicenseKeypadFlow.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/party/keypad/PersonNameKeypad.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/party/keypad/PhoneNumberKeypadFlow.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/party/keypad/partyKeypadFlowStyles.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `components/party/partyMobileWizardStyles.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `constants/ChatFilterChrome.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `constants/LedgerSyncPalette.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `contexts/DemoTabBarScrollContext.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ai/components/AIBadge.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ai/components/AIInsightsPanel.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ai/components/ClientRiskBadge.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ai/components/TripAIBadges.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ai/components/VehicleHealthBadge.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ai/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ai/services/ai.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ai/types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/analytics/vehicle-economics/useVehicleEconomics.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/analytics/vehicle-economics/vehicleEconomicsEngine.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/analytics/vehicle-economics/vehicleEfficiencyRanking.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/analytics/vehicle-economics/vehicleProfitability.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/business-pulse/components/pulseEnterpriseStyles.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/chat/components/shared/ChatDateDivider.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/chat/contexts/IntegratedChatContext.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/clients/components/AddClientModal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/clients/components/CustomersTab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/clients/components/EditClientModal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/clients/initialClientForDetail.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/clients/services/clientContracts.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/clients/services/clientWarehouses.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/clients/utils/clientContactDisplay.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/clients/utils/saleRateSnapshot.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/compliance/services/documents.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/compliance/types/compliance.types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/compliance/utils/complianceScore.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/connections/hooks/usePartyPhoneLookupState.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/connections/services/relationshipService.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/drivers/components/AddDriverModal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/drivers/components/DriverRegistrationFormFields.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/drivers/components/DriverRegistrationPortalFlow.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/drivers/components/DriversTab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/drivers/components/analytics/FleetDriverAnalyticsTab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/drivers/initialDriverForDetail.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/aggregation/aggregateCustomers.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/aggregation/aggregateCustomersFromRpc.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/aggregation/aggregateDcoPayeesFromRpc.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/aggregation/aggregateDriversFromRpc.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/aggregation/aggregateSuppliers.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/aggregation/aggregateSuppliersFromRpc.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/aggregation/driverMonthlyStatement.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/aggregation/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/aggregation/types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/DcoPayeesTab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/EntityDetailOverlay.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/EntityListCategoryModal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/FinanceAIInsights.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/FinanceAnalyticsView.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/FinanceKanbanTab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/FinanceModals.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/FinanceModalsGate.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/FinancePartyRegistrationPortal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/FinancePromoCard.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/FinanceScreen.styles.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/FinanceScreen.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/FinanceSummarySection.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/FinanceTabBody.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/FinanceTabBody.types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/FinanceTabRow.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/FinancialRow.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/LedgerBlock.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/LedgerDayDivider.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/LedgerExpandedCard.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/LedgerFlowChip.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/LedgerReportModal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/LedgerTab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/LedgerTransactionListView.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/LedgerTransactionPreviewModal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/PartyRegistrationPortal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/TreasuryDetailLayout.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/TreasurySummaryBanner.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/TreasurySummaryCard.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/TripPnLDetailSheet.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/finance-tabs/FinanceCashKanbanPanel.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/finance-tabs/FinanceCashLedgerPanel.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/finance-tabs/FinanceCashTab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/finance-tabs/FinanceCustomersTab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/finance-tabs/FinanceDriversTab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/finance-tabs/FinanceGarageTab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/finance-tabs/FinanceSuppliersTab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/ledger/buildFinancialRowDataForLedgerRow.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/components/ledger/ledgerTransactionLayout.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/constants/tableColumns.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/domain/tripCommercialAdjustment.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/domain/tripCostEvent.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/hooks/useFinanceAddEntityHandlers.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/hooks/useFinanceEntities.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/hooks/useFinanceLedger.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/hooks/useFinanceTransactionSubmit.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/hooks/useRealtimeTransactions.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/ledger/tripLedgerEntryChooser.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/lib/entityDetailReports.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/lib/filterLedgerByPeriod.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/lib/garageReportTransactions.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/lib/ledgerReportTable.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/lib/ledgerTotals.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/lib/partyRosterReport.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/mappers/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/selectors/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/services/ledgerAggregationRpc.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/services/tripSubcontracts.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/utils/allocateToLargestDue.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/utils/computeTripEntryFinancials.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/utils/getTripLedgerEntries.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/utils/ledgerDerivedPaidSeed.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/utils/ledgerTransactionReceipt.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/utils/partnerIndentFreightCost.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/finance/utils/tripLedgerPayoutMode.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/fleet/domain/VehicleExpenseEvent.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/fleet/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/fleet/mappers/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/fleet/mappers/vehicleExpenseEventMappers.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/fleet/selectors/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/fleet/selectors/profitabilitySelectors.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/fleet/selectors/vehicleAccountingSelectors.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/identity/avatars/IdentityAvatar.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/identity/cache/identityCache.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/identity/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/identity/queries/useIdentityQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/identity/selectors/identitySelectors.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/identity/trust/trustModel.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/identity/types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/identity/utils/identityUtils.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/indents/services/direct-quotes.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/indents/services/indents.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/indents/utils/bidding/indentReviewHubOffers.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/indents/utils/findIndentInList.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/indents/utils/indentCreateTimeout.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/indents/utils/indentShareCopies.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/indents/utils/indentVehicleCount.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/invoicing/utils/invoicePodPolicy.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/marketplace/domain/commercialPricing.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/network/components/desktop/networkDesktopHub.styles.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/network/constants/networkHubGrid.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/network/services/bids.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/network/services/fetchExecutionPlanRouteSummaries.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/network/services/indentStoryPosts.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/network/services/story-views.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/network/utils/commercePlanIds.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/network/utils/executionPlanRouteSummary.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/network/utils/indentStoryWindow.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/network/utils/partyContactDisplay.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/network/utils/storyReplyPreview.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ocr/constants/ocr.constants.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ocr/constants/pulseScanEngine.constants.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ocr/hooks/useOcrJobPoll.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ocr/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ocr/services/ocrJobProcessor.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ocr/services/ocrQuota.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ocr/services/pulseScanEngine.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ocr/services/vehicleOdometerEvent.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ocr/types/ocr.types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ocr/utils/ocrConfidenceReview.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ocr/utils/ocrNotification.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ocr/utils/ocrRescan.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/control-center/ApprovalQueue.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/control-center/OperationalAlertsPanel.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/control-center/OperationsControlCenter.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/control-center/ReconciliationQueue.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/control-center/ReimbursementQueue.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/control-center/hooks/useOperationsBatchActions.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/control-center/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/control-center/queries/controlCenter.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/control-center/queries/useOperationsControlCenter.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/control-center/selectors/buildQueueSections.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/control-center/types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/observability/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/observability/operationalHealthEngine.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/observability/operationsObservability.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/observability/postingHealth.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/observability/queueHealth.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/observability/syncHealth.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/observability/useOperationalHealthSnapshot.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/operations/observability/useOperationalObservability.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/organization/services/members.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/organization/services/teamInvitePrecheck.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ratings/components/TripRatingsBlock.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ratings/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/ratings/types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/suppliers/components/AddSupplierModal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/suppliers/components/EditSupplierModal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/suppliers/components/SuppliersTab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/suppliers/initialSupplierForDetail.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/suppliers/services/suppliers.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/tracking/broadcast/TrackingBroadcastSubscriptionManager.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/tracking/hooks/useTrackingTripBroadcast.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/tracking/services/tripCheckpoints.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/tracking/types/broadcast.types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/tripCompliance/services/vehicleDocumentReuse.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/TripAssignmentBlock.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/add-trip/AddTripWizardProgress.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/add-trip/AggregateTrackingMobileStep.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/add-trip/DriverPhoneRecommendations.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/add-trip/addTripPulseTheme.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/add-trip/types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/add-trip/useAddTripForm.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/allocation/TripPhoneAssignmentWizard.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/allocation/TripPhoneReassignContextRail.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/allocation/tripPhoneAssignmentWizardSteps.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/assignment/AssignmentFlowShell.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/assignment/FleetAssignWorkspace.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/assignment/TripAssignmentWorkspace.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/assignment/assignmentFlowFooter.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/assignment/tripAssignmentWorkspace.styles.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/reassign/reassign.types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/trip-detail/TripDetailFinanceView.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/trip-detail/adjustment/ProvisionCnDnImpactTag.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/trip-detail/adjustment/provisionCnDnImpact.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/trip-detail/completedTripInitialLoad.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/trip-detail/hooks/useTripDetail.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/trip-detail/hooks/useTripRatings.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/components/trip-detail/tripDetail.helpers.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/hooks/useAggregateDriverPhoneLookup.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/hooks/useRealtimeTrips.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/hooks/useRequestDriverPing.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/hooks/useTripLiveTracking.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/initialTripForDetail.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/fuel/FuelEntryScreen.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/health/operationalHealth.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/hub/ExpensePreviewSheet.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/hub/OperationsHub.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/hub/TripExpensesScreen.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/maintenance/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/maintenance/useTripMaintenance.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/metrics/operationsMetrics.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/other/OtherExpenseEntryScreen.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/reimbursement/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/reimbursement/reimbursementEngine.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/reimbursement/reimbursementMetrics.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/reimbursement/reimbursementSelectors.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/reimbursement/reimbursementTimeline.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/reimbursement/useReimbursementQueue.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/DriverExpenseCategorySwitch.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/DriverExpenseChipSelect.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/DriverExpenseEntryLayout.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/DriverOpsEntryFooter.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/ExpenseBillPhotoScan.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/ExpenseBillScanBanner.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/ExpenseBillScanOverlay.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/OpsEntryBodyPhotoSlot.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/applyExpenseReceiptOcr.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/driverExpenseChipVisuals.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/driverOpsEntry.styles.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/expenseBillScan.constants.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/expenseBillScan.types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/expenseEntryPartyPreview.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/expenseReceiptOcr.parse.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/expenseReceiptOcr.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/operationsEntryScreen.styles.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/tripExpenseEntryExit.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/useExpenseBillCapture.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/shared/useLeaveTripExpenseEntry.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/state/useOperationsSyncState.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/summary/TripOperationsSummary.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/toll/TollEntryScreen.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/vehicle/syncPostedExpensesToOperationLedger.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/vehicle/useVehicleOperationLedgerPipeline.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/operations/vehicle/useVehicleOperationsLedger.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/services/moverAssetPayment.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/services/tripAdjustments.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/styles/assignmentShellShared.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/utils/driverPhoneLookup.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/utils/tripLocationDisplay.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/DistanceComparisonCard.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/OdometerEntryScreen.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/OdometerPhotoCapture.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/OdometerStartEndScreen.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/OdometerTimelineEvent.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/TripVerificationSummary.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/applyOdometerScan.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/components/OdometerEntryShell.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/components/OdometerKeypadFlow.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/components/OdometerNotesField.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/components/OdometerScanBanner.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/hooks/useHydrateOdometerPhotos.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/hooks/useOdometerPhotoOcr.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/hooks/useTripVerification.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/odometerPhotoOcr.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/odometerScan.types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/state/useVerificationSyncState.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/trips/verification/types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/vehicles/components/AddVehicleModal.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/vehicles/components/GarrageTab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/vehicles/components/VehicleAvatar.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/vehicles/components/analytics/AnalyticsChart.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/vehicles/components/analytics/FleetAnalyticsTab.tsx` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/vehicles/components/analytics/analyticsUtils.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/vehicles/initialVehicleForDetail.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/vehicles/pnl/garragePnL.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/vehicles/pnl/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/vehicles/pnl/vehicleProfitabilityBreakdown.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/vehicles/services/vehicleDocuments.service.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `features/vehicles/utils/vehicleDocuments.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/cache/deltaTypes.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/capabilities.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/chatUnreadSignal.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/contactPicker.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/contactPickerNative.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/contactPickerWeb.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/crypto/sha256.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/crypto/sha256File.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/dateRangePresets.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/emptyStateLottieAssets.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/entityIdentity.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/fabIconAssets.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/fabLottieAssets.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/financePromoAssets.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/financePromoLottieAssets.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/gemini/geminiVisionJson.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/gemini/resolveGeminiApiKey.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/globalSync/inboundProtocol.types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/globalSync/types.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/hooks/appQueryGate.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/hooks/connectionRequestQueryGate.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/hooks/useAppQueryGate.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/hooks/useConnectionRequestsFromGlobalSync.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/hooks/useDebouncedValue.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/hooks/useGlobalFabAnimation.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/hooks/useQueryBootDefer.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/hooks/useViewportHeight.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/indianDrivingLicenseInput.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/invalidateFleetDriverConnectionCaches.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/layoutInsets.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/ledgerFocusedTripTransactions.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/ledgerPartySmartTagPolicy.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/ledgerReconLottieAssets.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/ledgerTripSettlementPreview.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/localTripSubcontracts.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/lottieSource.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/media/captureImage.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/onboarding/constants.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/partyEntityAccent.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/paymentModes.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/personNameKeypad.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/platform/types/client-hub-profile.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/platform/types/master-data.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/pod/ocr.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/pod/prompts.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/productRegistry.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/pulseReportPrint.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/entityListQueryOptions.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/fetchEntityListWithFallback.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/fetchTripLiveTrackingSeed.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/index.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useBidsQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useClientsQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useDriverProfileImagesQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useDriversQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useFinanceEntityQueries.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useIndentsQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useLedgerAggregationQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useLinkedOrgDisplayQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useNetworkQueries.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useOrgMembersQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/usePostsQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useRealtimeInvalidation.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useStoryViewsQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useSuppliersQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useTransactionsQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useTripDetailBundleQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useTripFinanceAdjustmentsQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useTripLiveTrackingSeedQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useTripOperationsQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useTripVerificationQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useTripsQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/queries/useVehiclesQuery.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/safeAreaWeb.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/styles/createStyles.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/suite/productLock.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/suite/suiteProductModule.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/suite/suiteProducts.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/useCapabilities.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/useLinkedOrgProfileMap.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/useMemberAccess.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/usePaginatedScroll.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/useWebLayoutWidth.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/webPrint.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `lib/wizardLayout.util.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `types/pod.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
| `types/workspace.ts` | Y1 | driver imports only its types (extract types in Phase 2; the file stays) |
