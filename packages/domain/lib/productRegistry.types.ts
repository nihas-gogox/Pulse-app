// Types extracted from lib/productRegistry.ts (driver extraction, Phase 2, D19). Types only — no runtime code.

// ── Product identifiers ──────────────────────────────────────────────────────

export type ProductId =
  | 'pulse_core'
  | 'pulse_driver'
  | 'pulse_network'
  | 'pulse_network_bidding'
  | 'pulse_chat'
  | 'pulse_pod_pro'
  | 'pulse_invoice_pro'
  | 'pulse_finance_pro'
  | 'pulse_fleet_pro'
  | 'pulse_people'
  | 'pulse_talent'
  | 'pulse_marketplace'
  | 'pulse_exchange'
  | 'pulse_compliance'
  | 'pulse_ai'
  | 'pulse_reach';
