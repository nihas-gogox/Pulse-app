// Types extracted from lib/partyEntityAccent.ts (driver extraction, Phase 2, D19). Types only — no runtime code.

export type PartyEntityAccent = {
  ring: string;
  glow: string;
  glowCore: string;
  tint: string;
};
