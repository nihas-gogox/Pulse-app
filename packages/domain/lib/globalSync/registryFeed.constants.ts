/** Registry bell — page size (UI) and bootstrap fetch caps (DB-safe). */
export const REGISTRY_PAGE_SIZE = 15;

/** Initial salary rows loaded with global bootstrap (≈3 pages). */
export const REGISTRY_BOOTSTRAP_SALARY_LIMIT = 45;

/** Extra salary rows per “load more” when in-memory feed is exhausted. */
export const REGISTRY_LOAD_MORE_SALARY_LIMIT = 15;
