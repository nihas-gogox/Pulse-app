/**
 * Pulse design system — import from `@/design-system` for new UI.
 * Legacy screens may still use `@/constants/Theme` during migration.
 */
export { space, screenPaddingX, sectionGap, touchTargetMin } from './spacing';
export { typography, tabularNums } from './typography';
export { radius } from './radius';
export { elevation } from './elevation';
export { colors } from './colors';
export { motion, motiTransition } from './motion';
export { layout } from './layout';
export { density, type DensityTier } from './density';
